version: '3.8'

networks:
  pfcs-network:
    external: true

services:
  backend:
    build: .
    container_name: pfcs-backend
    ports:
      - "8001:8000"
    env_file: .env
    networks:
      - pfcs-network
    restart: always
